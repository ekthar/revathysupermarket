import { auth } from "@/auth"; import { prisma } from "@/lib/prisma"; import { canViewReports } from "@/lib/authz"; import { SupportInboxClient } from "@/components/admin/support-inbox-client";
export const dynamic = "force-dynamic";
export default async function AdminSupportPage() { const session = await auth(); if (!canViewReports(session?.user?.role)) return <p className="rounded-2xl bg-red-50 p-4 text-red-700">You do not have access to support.</p>; const tickets = await prisma.supportTicket.findMany({ include: { customer: { select: { name: true, phone: true } }, messages: { select: { id: true, body: true, createdAt: true }, orderBy: { createdAt: "asc" } } }, orderBy: [{ priority: "desc" }, { updatedAt: "desc" }], take: 100 }); return <div><h1 className="font-display text-3xl font-black">Support inbox</h1><p className="mt-1 text-sm text-muted-foreground">Order-linked customer conversations and resolution history.</p><div className="mt-5"><SupportInboxClient initialTickets={tickets.map((ticket) => ({ ...ticket, messages: ticket.messages.map((message) => ({ ...message, createdAt: message.createdAt.toISOString() })) }))} /></div></div>; }
