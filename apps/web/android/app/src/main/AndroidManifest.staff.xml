<?xml version="1.0" encoding="utf-8"?>
<!--
  Additional permissions for the STAFF app variant.
  Merged into AndroidManifest.xml at build time by build-capacitor.yml.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Exact alarms: wake phone for new order alerts -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Full-screen intent: show alarm over lock screen -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Boot completed: re-register pending alarms after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground service: keep notification listener alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Battery optimization: request exemption so alerts work reliably -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

</manifest>
