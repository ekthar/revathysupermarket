"use client";

// Client-side interactions for the order detail page
// (status changes, driver assignment, notes, etc.)
// This is a placeholder — the server component handles the full display
export function OrderDetailClient() {
  return null;
}
