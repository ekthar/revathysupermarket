"use client";
import { useState } from "react";
import { useToast } from "@/components/toast-provider";
type Ticket = { id: string; ticketNumber: string; subject: string; status: string; priority: string; customer: { name: string | null; phone: string | null }; messages: Array<{ id: string; body: string; createdAt: string }> };
export function SupportInboxClient({ initialTickets }: { initialTickets: Ticket[] }) { const { showToast } = useToast(); const [tickets, setTickets] = useState(initialTickets); const [replies, setReplies] = useState<Record<string, string>>({}); const [loading, setLoading] = useState<string | null>(null); async function update(ticket: Ticket, status: string) { setLoading(ticket.id); const response = await fetch("/api/admin/support", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id: ticket.id, status, reply: replies[ticket.id] || undefined }) }); const data = await response.json().catch(() => ({})); setLoading(null); if (!response.ok) return showToast(data.error ?? "Ticket update failed", "error"); setTickets((current) => current.map((entry) => entry.id === ticket.id ? { ...entry, status } : entry)); setReplies((current) => ({ ...current, [ticket.id]: "" })); showToast("Support ticket updated", "success"); } return <div className="grid gap-3">{tickets.length === 0 ? <p className="rounded-3xl border border-dashed border-border p-10 text-center text-muted-foreground">Support inbox is clear.</p> : tickets.map((ticket) => <article key={ticket.id} className="rounded-3xl border border-border bg-card p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-black uppercase text-primary">{ticket.ticketNumber} · {ticket.priority}</p><h2 className="mt-1 font-display text-xl font-black">{ticket.subject}</h2><p className="text-sm text-muted-foreground">{ticket.customer.name || ticket.customer.phone || "Customer"}</p></div><span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-black text-primary">{ticket.status.replaceAll("_", " ")}</span></div><div className="mt-3 rounded-2xl bg-muted p-3 text-sm">{ticket.messages.map((message) => <p key={message.id} className="py-1">{message.body}</p>)}</div><textarea value={replies[ticket.id] || ""} onChange={(event) => setReplies((current) => ({ ...current, [ticket.id]: event.target.value }))} placeholder="Reply to customer" className="mt-3 min-h-20 w-full rounded-2xl border border-border bg-background p-3 text-sm" /><div className="mt-2 flex flex-wrap gap-2"><button disabled={loading === ticket.id} onClick={() => update(ticket, "IN_PROGRESS")} className="h-10 rounded-xl bg-primary px-3 text-xs font-black text-white">Reply / In progress</button><button disabled={loading === ticket.id} onClick={() => update(ticket, "RESOLVED")} className="h-10 rounded-xl bg-emerald-100 px-3 text-xs font-black text-emerald-700">Resolve</button></div></article>)}</div>; }
